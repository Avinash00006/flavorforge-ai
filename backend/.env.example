# Server configuration (default: 5000)
PORT=5000

# Frontend origin (for CORS configuration)
FRONTEND_URL=http://localhost:3000

# MongoDB Atlas Connection URI
MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/flavorforge-ai?retryWrites=true&w=majority
